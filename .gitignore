node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env*.local
.env.deploy

# CDK build output. cdk.context.json is deliberately NOT ignored: it pins the
# hosted zone lookup so CI synthesizes the same template every time.
cdk/cdk.out/

# Baseline screenshots from scripts/shoot.mjs. Regenerate rather than commit:
# the PNGs are large, and they only compare against others taken on the same
# platform and browser build.
shots/
